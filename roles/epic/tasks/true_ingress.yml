---
- name: download True Ingress artifacts from gitlab
  get_url:
    headers:
      PRIVATE-TOKEN: "{{ gitlab_toby_ansible_ro }}"
    url: "{{ true_ingress_artifact }}"
    dest: /root
- name: untar artifacts
  unarchive:
    src: /root/true-ingress.tar.bz2
    dest: "{{ true_ingress_remote_path }}"
    extra_opts:
    - --strip-components=1
    remote_src: yes

- name: mount BPF filesystem
  mount:
    state: mounted
    fstype: bpf
    src: bpf
    path: /sys/fs/bpf
